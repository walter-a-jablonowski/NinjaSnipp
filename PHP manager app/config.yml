# Common config
# see also --local config-- and user settings

# Folder under users/ holding settings and lists. Override in config.local.yml
# (not committed) to keep a personal setup out of the repository.
user: default

debug:
  on: true
  showAllFileBtns: false

special: true
