sh: sql--
usage: |
  Generic SQL CRUD operations snippet.
  Choose operation and provide table and columns.

content: |
  -- {{ operation=SELECT|INSERT|UPDATE|DELETE }} on {{ table=users }}
  {{ operation=SELECT }} {{ columns= id, name, email }}
  FROM {{ table }}
  WHERE {{ where=id = :id }};
  
  -- Example parameters
  -- :id = {{ id=1 }}
