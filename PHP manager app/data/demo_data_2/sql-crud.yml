sh: sql--
usage: |
  Generic SQL CRUDs snippet.
  Choose action and provide table and columns.

content: |
  -- {{ action=SELECT|INSERT|UPDATE|DELETE }} on {{ table=users }}
  {{ action=SELECT }} {{ columns= id, name, email }}
  FROM {{ table }}
  WHERE {{ where=id = :id }};
  
  -- Example parameters
  -- :id = {{ id=1 }}
